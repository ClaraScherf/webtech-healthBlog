spring:
  datasource:
    username: pmoimrmvntywei
    password: d4f6695ccf635d15f89d35227e8d9bb02c91146dbe484e1b72bb57361481378f
    url: jdbc:postgresql://ec2-34-251-233-253.eu-west-1.compute.amazonaws.com:5432/df5oo8r5vc521a
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
  jpa:
    properties:
      hibernate:
        # The SQL dialect makes Hibernate generate better SQL for the chosen database
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          time_zone: UTC
    show-sql: false # Set true for debugging
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update # (create, create-drop, validate, update)
    open-in-view: false
  sql:
    init:
      platform: postgres
      mode: always